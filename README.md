# marketplace-currency-dashboard

Deploy repo for the **currency-dashboard** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs currency-dashboard --push`.

Coolify: base_directory `/`, dockerfile `/apps/currency-dashboard/Dockerfile`.
